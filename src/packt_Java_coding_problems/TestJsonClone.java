package packt_Java_coding_problems;

public class TestJsonClone {

/*

    private   <T> jsonClone(T t) {
        Gson gson=new Gson();
        String json=gson.toJson(t);
        return (T) gson.fromJson(json,t.getClass());

    }

    */

}
